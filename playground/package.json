{
  "name": "zwgsl-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-wasm": "cd .. && zig build wasm && mkdir -p playground/src/generated && cp zig-out/bin/zwgsl.wasm playground/src/generated/zwgsl.wasm",
    "dev": "npm run sync-wasm && vite",
    "build": "npm run sync-wasm && vite build"
  },
  "dependencies": {
    "monaco-editor": "^0.54.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.0.6"
  }
}
